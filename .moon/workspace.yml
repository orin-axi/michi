$schema: "https://moonrepo.dev/schemas/workspace.json"

projects:
  - "crates/*"
  - "packages/*"
  - "."

vcs:
  client: git
  defaultBranch: main
  sync: true
  hooks:
    pre-commit:
      - "just pre-commit"
    pre-push:
      - "just pre-push"

telemetry: false
