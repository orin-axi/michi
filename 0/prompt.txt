Report what you found

---

What are the unresolved issues? Let's also make look into cleaning up the architectural and structural gaps. Consider these all together and give me details. Think deeply and comprehensively

---

What do we do about 1 and 2? How do we resolve it?

---

Should we break up the module in some more semantic way? If it's too big to fit and implement it's too big  period, right?

---

<task-notification>
<task-id>a330fb975de39caca</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-gabe-Projects-michi/8d4c6337-231f-4089-8c21-3efca50f628c/tasks/a330fb975de39caca.output</output-file>
<status>completed</status>
<summary>Agent "Plan SPEC-ARCH-003 implementation" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Understood — here is the exact absolute path, and the complete plan@1 JSON content pasted directly below.

**File path:** `/private/tmp/claude-501/-Users-gabe-Projects-michi/e7a8609a-874c-4e5a-bfea-81d33b8e2904/scratchpad/plan.json`

```json
{
  "goal": "Eliminate unvalidated numeric coercion at the rendering-domain NAPI boundary by introducing validating newtypes (JsCount, JsInt, JsRanged/JsDecimals, JsFloat) that reject out-of-domain JS numbers before any michi rendering code runs, migrating all seven rendering call sites to them, and proving the fix with Rust unit tests, a compiled-addon Node test suite, and updated docs.",
  "architecture": "A new shared-kernel module src/napi/num.rs defines five validating newtypes, each implementing TryFrom&lt;f64&gt; plus the four napi-rs traits (TypeName, ValidateNapiValue, FromNapiValue, ToNapiValue) required for use as a #[napi] field/argument type. src/napi.rs then swaps its seven raw-primitive positions (JsToonValue's three fields, JsToonOptions.total_count, render_kv's total_count, truncate's max_chars, JsAgentResponse::total_count) for these newtypes, deleting every open-coded clamp/cast at each site, and gains a module-level clippy deny-lint to make the deletion permanent. Behavior is verified at three layers: Rust unit tests in src/napi/num.rs (domain boundaries of each newtype), Rust tests in src/napi.rs mod tests (the eleven migrated call sites, the lint/residual-coercion scan, and the docs/supersession consistency check), and Node tests against the compiled addon in packages/michi-node/__test__/index.test.mjs (cross-entry-point rejection agreement, NaN/fractional/out-of-range rejection, and the generated index.d.ts surface).",
  "tech_stack": ["Rust 2021 (rustc 1.96 pinned)", "napi-rs 3.10 / napi-derive 3", "cargo nextest", "clippy", "Node.js test runner (node:test)", "@napi-rs/cli build-node"],
  "linked_requirement": "FINDING-NAPI-NUMERIC-BOUNDARY-NARROW-INCONSISTENT-I32-COERCION",
  "spec_file_path": ".claude/specs/napi-rendering-boundary-newtypes.json",
  "spec_hash": "sha256:5480a2e88e5891c94272eac55155f103ed0836abf0b4e8c73540d7639d2b53e1",
  "reasoning": "Split into 9 tasks: (1)-(2) build the num.rs module in isolation, no dependency on src/napi.rs's existing call sites, split at the JsCount/JsInt vs JsRanged/JsFloat boundary because the generic get_u8 body is the highest-risk code in the whole spec and deserves its own commit. (3)-(4) migrate the 7 call sites; split at JsToonValue+JsToonOptions (which share test-literal edits at the same lines) vs render_kv/truncate/JsAgentResponse::total_count (which have no shared test literals and include the one test deletion). (5) lint deny + residual scan is deliberately last among Rust-only tasks because it can only pass once every cast-allow is actually gone. (6)-(8) are Node/addon tasks gated behind just build-node, kept separate because AC-008 (cross-entry-point agreement) is materially different in shape from AC-009 (five independent rejection/default assertions) and AC-012 (a static-string surface check) even though all three touch the same test file. (9) docs+supersession lands last per the prompt's explicit ordering guidance, and only after (4) so the docs describe the final call-site behavior. Every task's red phase for the two module-construction tasks (1, 2) is a compile error (E0433, type not found) since the newtype and its test are introduced together; the four migration tasks (3, 4) use the type-change itself as the red phase (E0308 mismatched types at named line numbers) with the existing test suite as the regression net, since the spec is explicit that these eleven sites must be migrated, not newly tested. All eleven compile-forced sites and the one deleted test are accounted for across tasks 3 and 4.",
  "tasks": [
    {
      "id": "T1-num-count-int",
      "title": "Create src/napi/num.rs with JsCount and JsInt",
      "depends_on": [],
      "covers_criteria": ["AC-001", "AC-002", "AC-003", "AC-004"],
      "files": {
        "create": ["src/napi/num.rs"],
        "modify": ["src/napi.rs"]
      },
      "steps": [
        "RED — In src/napi.rs, immediately after the `use napi_derive::napi;` line, insert:\n\n/// Validating newtypes for the NAPI numeric boundary (shared kernel for\n/// both the rendering and resilience domains).\npub mod num;\n\nThen create src/napi/num.rs with ONLY this content (module doc plus a test module that references JsCount and JsInt before they exist):\n\n//! Validating newtypes for the NAPI numeric boundary.\n//!\n//! Every JavaScript number that crosses into a `#[napi]` export in\n//! [`crate::napi`] is read as an `f64` via `napi_get_value_double` (a\n//! non-coercing read — `napi_get_value_int32`'s `ToInt32` wraparound is\n//! never in the path) and then validated through one of this module's\n//! `TryFrom&lt;f64&gt;` impls before any michi code runs. A `TryFrom&lt;f64&gt;`\n//! failure becomes a thrown `napi::Error` before the wrapped function body\n//! runs, so there is no coercion step left at any call site to reimplement\n//! inconsistently. This is the shared kernel for the whole NAPI numeric\n//! boundary: [`JsRanged`] (added in a later commit) is generic over its\n//! bounds so a future resilience-domain module can define its own aliases\n//! over the same type without redefining it.\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn js_count_accepts_valid_domain() {\n        for (v, expected) in [\n            (0.0, 0usize),\n            (1.0, 1),\n            (42.0, 42),\n            (1e6, 1_000_000),\n            (9_007_199_254_740_991.0, 9_007_199_254_740_991),\n        ] {\n            assert_eq!(JsCount::try_from(v).map(JsCount::get), Ok(expected), \"input {v}\");\n        }\n    }\n\n    #[test]\n    fn js_count_rejects_out_of_domain() {\n        for v in [-1.0, -0.5, 0.5, 1.5, f64::NAN, f64::INFINITY, f64::NEG_INFINITY, 9_007_199_254_740_992.0] {\n            let err = JsCount::try_from(v).expect_err(\"out-of-domain value must be rejected\");\n            assert_eq!(err, format!(\"expected a non-negative integer no greater than 9007199254740991, got {v}\"));\n        }\n    }\n\n    #[test]\n    fn js_int_accepts_safe_integers() {\n        for (v, expected) in [\n            (0.0, 0i64),\n            (-1.0, -1),\n            (1.0, 1),\n            (-9_007_199_254_740_991.0, -9_007_199_254_740_991),\n            (9_007_199_254_740_991.0, 9_007_199_254_740_991),\n            (-1_755_000_000_000.0, -1_755_000_000_000),\n        ] {\n            assert_eq!(JsInt::try_from(v).map(JsInt::get), Ok(expected), \"input {v}\");\n        }\n    }\n\n    #[test]\n    fn js_int_rejects_fractional_and_unsafe() {\n        for v in [1.5, -1.5, f64::NAN, f64::INFINITY, f64::NEG_INFINITY, 9_007_199_254_740_992.0, -9_007_199_254_740_992.0] {\n            let err = JsInt::try_from(v).expect_err(\"out-of-domain value must be rejected\");\n            assert_eq!(err, format!(\"expected an integer in [-9007199254740991, 9007199254740991], got {v}\"));\n        }\n    }\n}",
        "RED — Run `cargo check --features napi --lib --tests` from the repo root. Expected failure: compile errors `cannot find type \\`JsCount\\` in this scope` and `cannot find type \\`JsInt\\` in this scope` in src/napi/num.rs, one per test function that references them. This confirms the test module compiles against the crate but the types it needs don't exist yet.",
        "GREEN — Insert the following above the `#[cfg(test)] mod tests {` line in src/napi/num.rs (i.e. between the module doc comment and the test module):\n\nuse napi::bindgen_prelude::{sys, FromNapiValue, ToNapiValue, TypeName, ValidateNapiValue};\n\n/// A JavaScript number accepted only when it is finite, has no fractional\n/// part, is `&gt;= 0`, and is `&lt;= 9007199254740991`\n/// (`Number.MAX_SAFE_INTEGER`). Stores the validated value as `usize`.\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct JsCount(usize);\n\nimpl JsCount {\n    /// Returns the validated count.\n    #[must_use]\n    pub const fn get(self) -&gt; usize {\n        self.0\n    }\n}\n\nimpl TryFrom&lt;f64&gt; for JsCount {\n    type Error = String;\n\n    fn try_from(v: f64) -&gt; Result&lt;Self, Self::Error&gt; {\n        if v.is_finite() &amp;&amp; v.fract() == 0.0 &amp;&amp; v &gt;= 0.0 &amp;&amp; v &lt;= 9_007_199_254_740_991.0 {\n            Ok(Self(v as usize))\n        } else {\n            Err(format!(\"expected a non-negative integer no greater than 9007199254740991, got {v}\"))\n        }\n    }\n}\n\nimpl TypeName for JsCount {\n    fn type_name() -&gt; &amp;'static str {\n        \"JsCount\"\n    }\n\n    fn value_type() -&gt; napi::ValueType {\n        napi::ValueType::Number\n    }\n}\n\nimpl ValidateNapiValue for JsCount {}\n\nimpl FromNapiValue for JsCount {\n    unsafe fn from_napi_value(env: sys::napi_env, napi_val: sys::napi_value) -&gt; napi::Result&lt;Self&gt; {\n        let v = unsafe { f64::from_napi_value(env, napi_val)? };\n        Self::try_from(v).map_err(|msg| napi::Error::new(napi::Status::InvalidArg, msg))\n    }\n}\n\nimpl ToNapiValue for JsCount {\n    unsafe fn to_napi_value(env: sys::napi_env, val: Self) -&gt; napi::Result&lt;sys::napi_value&gt; {\n        unsafe { f64::to_napi_value(env, val.0 as f64) }\n    }\n}\n\n/// A JavaScript number accepted only when it is finite, has no fractional\n/// part, and lies within `[-9007199254740991, 9007199254740991]` — the\n/// range over which a JS number and an `i64` agree exactly. Stores `i64`.\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct JsInt(i64);\n\nimpl JsInt {\n    /// Returns the validated integer.\n    #[must_use]\n    pub const fn get(self) -&gt; i64 {\n        self.0\n    }\n}\n\nimpl TryFrom&lt;f64&gt; for JsInt {\n    type Error = String;\n\n    fn try_from(v: f64) -&gt; Result&lt;Self, Self::Error&gt; {\n        if v.is_finite() &amp;&amp; v.fract() == 0.0 &amp;&amp; v &gt;= -9_007_199_254_740_991.0 &amp;&amp; v &lt;= 9_007_199_254_740_991.0 {\n            Ok(Self(v as i64))\n        } else {\n            Err(format!(\"expected an integer in [-9007199254740991, 9007199254740991], got {v}\"))\n        }\n    }\n}\n\nimpl TypeName for JsInt {\n    fn type_name() -&gt; &amp;'static str {\n        \"JsInt\"\n    }\n\n    fn value_type() -&gt; napi::ValueType {\n        napi::ValueType::Number\n    }\n}\n\nimpl ValidateNapiValue for JsInt {}\n\nimpl FromNapiValue for JsInt {\n    unsafe fn from_napi_value(env: sys::napi_env, napi_val: sys::napi_value) -&gt; napi::Result&lt;Self&gt; {\n        let v = unsafe { f64::from_napi_value(env, napi_val)? };\n        Self::try_from(v).map_err(|msg| napi::Error::new(napi::Status::InvalidArg, msg))\n    }\n}\n\nimpl ToNapiValue for JsInt {\n    unsafe fn to_napi_value(env: sys::napi_env, val: Self) -&gt; napi::Result&lt;sys::napi_value&gt; {\n        unsafe { f64::to_napi_value(env, val.0 as f64) }\n    }\n}",
        "GREEN — Run `cargo check --features napi --lib --tests` and confirm it exits 0 with no errors.",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(js_count_accepts_valid_domain) + test(js_count_rejects_out_of_domain) + test(js_int_accepts_safe_integers) + test(js_int_rejects_fractional_and_unsafe)'`. Expected: `4 tests run: 4 passed`.",
        "Run `cargo doc --no-deps --features napi 2&gt;&amp;1 | grep -i 'missing documentation'` (or just `just doc-check`) and confirm no `missing documentation` warning is emitted for `pub mod num`, `JsCount`, `JsCount::get`, `JsInt`, or `JsInt::get` — `#![warn(missing_docs)]` in src/lib.rs requires each of these five pub items to carry a doc comment, and they do."
      ],
      "commit_message": "feat(napi): add JsCount and JsInt validating newtypes for the NAPI numeric boundary"
    },
    {
      "id": "T2-num-ranged-float",
      "title": "Add generic JsRanged, JsDecimals alias, and JsFloat to src/napi/num.rs",
      "depends_on": ["T1-num-count-int"],
      "covers_criteria": ["AC-005", "AC-006", "AC-007"],
      "files": {
        "modify": ["src/napi/num.rs"],
        "create": ["tests/napi_num_reuse.rs"]
      },
      "steps": [
        "RED — Append the following three test functions inside the existing `#[cfg(test)] mod tests { ... }` block in src/napi/num.rs (before its closing `}`), referencing JsRanged/JsDecimals/JsFloat before they exist:\n\n    #[test]\n    fn js_decimals_accepts_zero_through_twenty() {\n        for (v, expected) in [(0.0, 0u8), (6.0, 6), (20.0, 20)] {\n            let d = JsDecimals::try_from(v).expect(\"in-domain decimals value\");\n            assert_eq!(d.get_u8(), expected, \"input {v}\");\n        }\n    }\n\n    #[test]\n    fn js_decimals_rejects_out_of_range() {\n        for v in [-1.0, 21.0, 6.5, f64::NAN, f64::INFINITY] {\n            let err = JsDecimals::try_from(v).expect_err(\"out-of-range decimals value must be rejected\");\n            assert_eq!(err, format!(\"expected an integer in [0, 20], got {v}\"));\n        }\n    }\n\n    #[test]\n    fn js_float_accepts_finite_rejects_nan_and_infinity() {\n        for v in [0.0, -0.0, 1.5, -1.5, f64::MAX, f64::MIN_POSITIVE] {\n            let f = JsFloat::try_from(v).expect(\"finite value must be accepted\");\n            assert_eq!(f.get().to_bits(), v.to_bits(), \"input {v}\");\n        }\n        for v in [f64::NAN, f64::INFINITY, f64::NEG_INFINITY] {\n            let err = JsFloat::try_from(v).expect_err(\"non-finite value must be rejected\");\n            assert_eq!(err, format!(\"expected a finite number, got {v}\"));\n        }\n    }\n\nAlso create tests/napi_num_reuse.rs with exactly:\n\n#![cfg(feature = \"napi\")]\n\n#[test]\nfn js_ranged_is_generic_over_bounds() {\n    let decimals = michi::napi::num::JsRanged::&lt;0, 20&gt;::try_from(6.0).expect(\"6 is in [0, 20]\");\n    assert_eq!(decimals.get_u8(), 6);\n\n    let status_like = michi::napi::num::JsRanged::&lt;100, 599&gt;::try_from(429.0).expect(\"429 is in [100, 599]\");\n    assert_eq!(status_like.get(), 429i64);\n}",
        "RED — Run `cargo check --features napi --lib --tests`. Expected failure: `cannot find type \\`JsDecimals\\` in this scope` and `cannot find type \\`JsFloat\\` in this scope` in src/napi/num.rs, plus `unresolved import \\`michi::napi::num::JsRanged\\`` (or `cannot find type \\`JsRanged\\` in module \\`michi::napi::num\\``) in tests/napi_num_reuse.rs.",
        "GREEN — Insert the following into src/napi/num.rs above the `#[cfg(test)]` line (after JsInt's ToNapiValue impl from T1):\n\n/// The shared kernel type for every bounded-integer position on the NAPI\n/// boundary, in either domain. Accepts a JS number only when it is finite,\n/// has no fractional part, and satisfies `MIN &lt;= v &lt;= MAX`. Generic and\n/// reusable — not specialized to any single domain's bounds.\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct JsRanged&lt;const MIN: i64, const MAX: i64&gt;(i64);\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; JsRanged&lt;MIN, MAX&gt; {\n    const U8_FITS: () = assert!(MIN &gt;= 0 &amp;&amp; MAX &lt;= u8::MAX as i64, \"JsRanged bounds do not fit in u8\");\n\n    /// Returns the validated integer.\n    #[must_use]\n    pub const fn get(self) -&gt; i64 {\n        self.0\n    }\n\n    /// Returns the validated integer narrowed to `u8`. Only valid when\n    /// `MIN &gt;= 0 &amp;&amp; MAX &lt;= u8::MAX`; instantiating this function with\n    /// bounds that don't fit `u8` is a compile error (`E0080`) via\n    /// `U8_FITS`.\n    // NOT `const fn`: `u8::try_from` cannot be called inside a const fn on\n    // this workspace's pinned rustc 1.96 (E0658 — `TryFrom` is not yet a\n    // const trait).\n    pub fn get_u8(self) -&gt; u8 {\n        let () = Self::U8_FITS;\n        match u8::try_from(self.0) {\n            Ok(v) =&gt; v,\n            Err(_) =&gt; u8::MAX,\n        }\n    }\n}\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; TryFrom&lt;f64&gt; for JsRanged&lt;MIN, MAX&gt; {\n    type Error = String;\n\n    fn try_from(v: f64) -&gt; Result&lt;Self, Self::Error&gt; {\n        if v.is_finite() &amp;&amp; v.fract() == 0.0 &amp;&amp; v &gt;= MIN as f64 &amp;&amp; v &lt;= MAX as f64 {\n            Ok(Self(v as i64))\n        } else {\n            Err(format!(\"expected an integer in [{MIN}, {MAX}], got {v}\"))\n        }\n    }\n}\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; TypeName for JsRanged&lt;MIN, MAX&gt; {\n    fn type_name() -&gt; &amp;'static str {\n        \"JsRanged\"\n    }\n\n    fn value_type() -&gt; napi::ValueType {\n        napi::ValueType::Number\n    }\n}\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; ValidateNapiValue for JsRanged&lt;MIN, MAX&gt; {}\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; FromNapiValue for JsRanged&lt;MIN, MAX&gt; {\n    unsafe fn from_napi_value(env: sys::napi_env, napi_val: sys::napi_value) -&gt; napi::Result&lt;Self&gt; {\n        let v = unsafe { f64::from_napi_value(env, napi_val)? };\n        Self::try_from(v).map_err(|msg| napi::Error::new(napi::Status::InvalidArg, msg))\n    }\n}\n\nimpl&lt;const MIN: i64, const MAX: i64&gt; ToNapiValue for JsRanged&lt;MIN, MAX&gt; {\n    unsafe fn to_napi_value(env: sys::napi_env, val: Self) -&gt; napi::Result&lt;sys::napi_value&gt; {\n        unsafe { f64::to_napi_value(env, val.0 as f64) }\n    }\n}\n\n/// The rendering-domain alias used at `JsToonValue.decimalsVal`. Consumed\n/// via `get_u8()`, which compiles because `[0, 20]` fits `u8`.\npub type JsDecimals = JsRanged&lt;0, 20&gt;;\n\n/// A JavaScript number accepted only when `f64::is_finite` holds — `NaN`,\n/// `Infinity`, and `-Infinity` are rejected. Stores `f64`.\n#[derive(Debug, Clone, Copy, PartialEq)]\npub struct JsFloat(f64);\n\nimpl JsFloat {\n    /// Returns the validated float.\n    #[must_use]\n    pub const fn get(self) -&gt; f64 {\n        self.0\n    }\n}\n\nimpl TryFrom&lt;f64&gt; for JsFloat {\n    type Error = String;\n\n    fn try_from(v: f64) -&gt; Result&lt;Self, Self::Error&gt; {\n        if v.is_finite() {\n            Ok(Self(v))\n        } else {\n            Err(format!(\"expected a finite number, got {v}\"))\n        }\n    }\n}\n\nimpl TypeName for JsFloat {\n    fn type_name() -&gt; &amp;'static str {\n        \"JsFloat\"\n    }\n\n    fn value_type() -&gt; napi::ValueType {\n        napi::ValueType::Number\n    }\n}\n\nimpl ValidateNapiValue for JsFloat {}\n\nimpl FromNapiValue for JsFloat {\n    unsafe fn from_napi_value(env: sys::napi_env, napi_val: sys::napi_value) -&gt; napi::Result&lt;Self&gt; {\n        let v = unsafe { f64::from_napi_value(env, napi_val)? };\n        Self::try_from(v).map_err(|msg| napi::Error::new(napi::Status::InvalidArg, msg))\n    }\n}\n\nimpl ToNapiValue for JsFloat {\n    unsafe fn to_napi_value(env: sys::napi_env, val: Self) -&gt; napi::Result&lt;sys::napi_value&gt; {\n        unsafe { f64::to_napi_value(env, val.0) }\n    }\n}",
        "GREEN — Run `cargo check --features napi --lib --tests` and confirm it exits 0.",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(js_decimals) + test(js_float) + test(js_ranged_is_generic_over_bounds)'`. Expected: `4 tests run: 4 passed` (js_decimals_accepts_zero_through_twenty, js_decimals_rejects_out_of_range, js_float_accepts_finite_rejects_nan_and_infinity, js_ranged_is_generic_over_bounds).",
        "Confirm `JsRanged::&lt;0, 300&gt;::get_u8` is NOT called anywhere in the codebase (it must remain a compile-time trap, not a runtime one) by running `grep -rn 'JsRanged::&lt;0, 300&gt;' src/ tests/` and confirming no output."
      ],
      "commit_message": "feat(napi): add generic JsRanged, JsDecimals alias, and JsFloat to the NAPI numeric boundary"
    },
    {
      "id": "T3-migrate-toonvalue-and-options",
      "title": "Migrate JsToonValue's three fields and JsToonOptions.total_count to the new newtypes",
      "depends_on": ["T2-num-ranged-float"],
      "covers_criteria": [],
      "files": {
        "modify": ["src/napi.rs"]
      },
      "steps": [
        "RED (the type change is the implementation under test) — In src/napi.rs, add `use self::num::{JsCount, JsDecimals, JsFloat, JsInt};` directly below `use napi_derive::napi;`. Change JsToonValue's three field declarations (currently `pub int_val: Option&lt;i64&gt;` at line 53, `pub float_val: Option&lt;f64&gt;` at line 56, `pub decimals_val: Option&lt;i32&gt;` at line 62) to:\n\n    #[napi(js_name = \"intVal\", ts_type = \"number\")]\n    pub int_val: Option&lt;JsInt&gt;,\n    /// The value when `type` is `\"float\"`.\n    #[napi(js_name = \"floatVal\", ts_type = \"number\")]\n    pub float_val: Option&lt;JsFloat&gt;,\n    /// The value when `type` is `\"bool\"`.\n    #[napi(js_name = \"boolVal\")]\n    pub bool_val: Option&lt;bool&gt;,\n    /// Decimal places when `type` is `\"float\"` (KV render only). Rejected if outside [0, 20]. Defaults to 6.\n    #[napi(js_name = \"decimalsVal\", ts_type = \"number\")]\n    pub decimals_val: Option&lt;JsDecimals&gt;,\n\n(preserving the `bool_val` field and its doc comment unchanged in between; only the attribute lines and types of int_val/float_val/decimals_val change, plus the decimals_val doc comment's 'Clamped to [0, 20]' becomes 'Rejected if outside [0, 20]').\n\nAlso change JsToonOptions.total_count (currently `pub total_count: Option&lt;i32&gt;` at line 78) to:\n\n    #[napi(js_name = \"totalCount\", ts_type = \"number\")]\n    pub total_count: Option&lt;JsCount&gt;,",
        "RED — Run `cargo check --features napi --lib --tests`. Expected failure: multiple `mismatched types` (E0308) errors, including at src/napi.rs:86-87 (`js_value_to_rust` reading `v.int_val`/`v.float_val` as raw i64/f64), src/napi.rs:136 (`.total_count(opts.total_count.map(usize::try_from)...)`), src/napi.rs:237-238 (`js_kv_value_to_rust` reading `v.decimals_val`/`v.int_val`), and in `mod tests` at the lines listed in the next step (745, 746, 806, 884, 927, 928, 939, 941, 1123, 1133).",
        "GREEN — Update `js_value_to_rust` (currently lines 83-91) so the \"int\" and \"float\" arms read:\n\n        \"int\" =&gt; michi_toon::Value::Int(v.int_val.map_or(0, JsInt::get)),\n        \"float\" =&gt; michi_toon::Value::Float(v.float_val.map_or(0.0, JsFloat::get)),\n\nUpdate `render_toon`'s ToonOptions construction (currently line 136) to:\n\n        .total_count(opts.total_count.map(JsCount::get))\n\nUpdate `js_kv_value_to_rust` (currently lines 232-243) so the \"int\" and \"float\" arms read:\n\n        \"int\" =&gt; crate::kv::KvValue::Int(v.int_val.map_or(0, JsInt::get)),\n        \"float\" =&gt; {\n            let decimals = v.decimals_val.map_or(6, JsDecimals::get_u8);\n            crate::kv::KvValue::Float(v.float_val.map_or(0.0, JsFloat::get), decimals)\n        }",
        "GREEN — Migrate the compile-broken test call sites in `mod tests`, matching each exactly: at line 745 change `int_val: Some(1)` to `int_val: Some(JsInt::try_from(1.0).expect(\"1 is a valid int\"))`; at line 746 (same struct literal) change `total_count: Some(1)` to `total_count: Some(JsCount::try_from(1.0).expect(\"1 is a valid count\"))`; at line 806 change `int_val: Some(1_755_000_000_000)` to `int_val: Some(JsInt::try_from(1_755_000_000_000.0).expect(\"1_755_000_000_000 is a valid int\"))`; at line 884 change `int_val: Some(1)` to `int_val: Some(JsInt::try_from(1.0).expect(\"1 is a valid int\"))`; at line 927 change `int_val: Some(1)` to the same JsInt form; at line 928 change `int_val: Some(99)` to `int_val: Some(JsInt::try_from(99.0).expect(\"99 is a valid int\"))`; at line 939 change `int_val: Some(1)` to the JsInt form for 1; at line 941 change `int_val: Some(99)` to the JsInt form for 99; at line 1123 change `float_val: Some(3.14159)` to `float_val: Some(JsFloat::try_from(3.14159).expect(\"3.14159 is finite\"))`; at line 1133 change `float_val: Some(3.14159), decimals_val: Some(2)` to `float_val: Some(JsFloat::try_from(3.14159).expect(\"3.14159 is finite\")), decimals_val: Some(JsDecimals::try_from(2.0).expect(\"2 is a valid decimals value\"))`.",
        "GREEN — Run `cargo check --features napi --lib --tests` and confirm it exits 0 with no errors.",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(render_toon) + test(js_agent_response_items) + test(js_agent_response_render) + test(render_kv_napi_float)'`. Expected: all matched tests pass (render_toon_basic, render_toon_renders_int_beyond_i32_range, js_agent_response_items_then_render_toon, js_agent_response_render_toon_is_slot_specific_even_after_kv_items_called_last, js_agent_response_render_kv_is_slot_specific_even_after_items_called_last, render_kv_napi_float_default_decimals, render_kv_napi_float_custom_decimals — 7 tests run: 7 passed)."
      ],
      "commit_message": "refactor(napi): migrate JsToonValue.intVal/floatVal/decimalsVal and JsToonOptions.totalCount to validating newtypes"
    },
    {
      "id": "T4-migrate-render-kv-truncate-agentresponse",
      "title": "Migrate render_kv, truncate, and JsAgentResponse::total_count to JsCount",
      "depends_on": ["T3-migrate-toonvalue-and-options"],
      "covers_criteria": [],
      "files": {
        "modify": ["src/napi.rs"]
      },
      "steps": [
        "RED (the type change is the implementation under test) — In src/napi.rs, change `truncate`'s signature (currently lines 216-220):\n\n#[napi(catch_unwind)]\n#[allow(clippy::needless_pass_by_value)] // napi-derive requires owned String for JS string params\npub fn truncate(content: String, #[napi(ts_arg_type = \"number\")] max_chars: JsCount, hint: String) -&gt; String {\n    crate::truncate::truncate_inline(&amp;content, max_chars.get(), &amp;hint)\n}\n\n(deleting the `#[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]` line that was at line 217, and the `.max(0) as usize` call).\n\nChange `render_kv`'s signature and body (currently lines 246-259):\n\n#[napi(catch_unwind)]\npub fn render_kv(items: Vec&lt;JsKvItem&gt;, #[napi(ts_arg_type = \"number | undefined | null\")] total_count: Option&lt;JsCount&gt;, hints: Vec&lt;String&gt;) -&gt; napi::Result&lt;String&gt; {\n    if items.len() &gt; MAX_FIELDS {\n        return Err(napi::Error::from_reason(format!(\"items length {} exceeds maximum of {MAX_FIELDS}\", items.len())));\n    }\n    if hints.len() &gt; MAX_HINTS {\n        return Err(napi::Error::from_reason(format!(\"hints length {} exceeds maximum of {MAX_HINTS}\", hints.len())));\n    }\n    let converted: Vec&lt;crate::kv::KvItem&gt; =\n        items.into_iter().map(|i| crate::kv::KvItem { key: i.key, value: js_kv_value_to_rust(i.value) }).collect();\n    let hint_objs: Vec&lt;crate::hints::Hint&gt; = hints.into_iter().map(Into::into).collect();\n    Ok(crate::kv::render_kv(&amp;converted, total_count.map(JsCount::get), &amp;hint_objs))\n}\n\n(deleting the `#[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]` line that was at line 247, and the `.map(|n| n.max(0) as usize)` call).\n\nChange `JsAgentResponse::total_count`'s signature and body (currently lines 514-520):\n\n    /// Set the total available count (TOON path only).\n    #[napi(catch_unwind)]\n    pub fn total_count(&amp;mut self, #[napi(ts_arg_type = \"number\")] n: JsCount) -&gt; napi::Result&lt;()&gt; {\n        let b = self.take()?;\n        self.inner = Some(b.total_count(n.get()));\n        Ok(())\n    }\n\n(deleting the `#[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)] // n clamped non-negative first` line that was at line 515, and the `n.max(0) as usize` call).",
        "RED — Run `cargo check --features napi --lib --tests`. Expected failure: `mismatched types` (E0308) at src/napi.rs:871 (`truncate(\"hello\".to_string(), 100, \"full=true\".to_string())` — `100` is `i32`, not `JsCount`) and unused-import or dead-code warnings are not expected to block compilation, but the build fails specifically on line 871's argument type.",
        "GREEN — At line 871, change `truncate(\"hello\".to_string(), 100, \"full=true\".to_string())` to `truncate(\"hello\".to_string(), JsCount::try_from(100.0).expect(\"100 is a valid count\"), \"full=true\".to_string())`.",
        "GREEN — Delete the `truncate_clamps_negative_max_chars` test in its entirety (the `#[test] fn truncate_clamps_negative_max_chars() { ... }` block, currently lines 874-879) — a negative `max_chars` can no longer be constructed as a `JsCount` in Rust at all, so this test is unwritable, not merely wrong; it is replaced by a Node-level rejection test in a later task (AC-009).",
        "GREEN — Run `cargo check --features napi --lib --tests` and confirm it exits 0 with no errors.",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(truncate) + test(render_kv) + test(js_agent_response)'`. Expected: every matched test passes and `truncate_clamps_negative_max_chars` no longer appears in the test list at all (confirm via `cargo nextest list --workspace --all-features -E 'test(truncate_clamps_negative_max_chars)'` printing zero tests)."
      ],
      "commit_message": "refactor(napi): migrate render_kv, truncate, and JsAgentResponse::totalCount to JsCount"
    },
    {
      "id": "T5-lint-deny-and-residual-scan",
      "title": "Deny residual cast lints and forbid open-coded numeric coercion",
      "depends_on": ["T4-migrate-render-kv-truncate-agentresponse"],
      "covers_criteria": ["AC-010", "AC-011"],
      "files": {
        "modify": ["src/napi.rs", "src/napi/num.rs"]
      },
      "steps": [
        "RED — Append the following two tests to the `#[cfg(test)] mod tests { ... }` block in src/napi/num.rs:\n\n    #[test]\n    fn module_denies_cast_lints() {\n        let src = include_str!(\"../napi.rs\");\n        assert!(\n            src.contains(\"#![deny(clippy::as_conversions, clippy::cast_possible_truncation, clippy::cast_sign_loss, clippy::cast_possible_wrap, clippy::cast_precision_loss)]\"),\n            \"expected the module-level deny attribute for cast lints\"\n        );\n    }\n\n    #[test]\n    fn boundary_carries_exactly_one_cast_allow_and_no_residual_coercion() {\n        let napi_src = include_str!(\"../napi.rs\");\n        let num_src = include_str!(\"num.rs\");\n        assert_eq!(\n            napi_src.matches(\"#[allow(clippy::cast_possible_truncation\").count(),\n            1,\n            \"expected exactly one cast-allow attribute (is_retryable_status) to remain in src/napi.rs\"\n        );\n        assert!(!num_src.contains(\"#[allow(clippy::cast_possible_truncation\"), \"src/napi/num.rs must carry zero cast-allow attributes\");\n        for residual in [\".max(0)\", \".clamp(\", \"usize::try_from\"] {\n            assert!(!napi_src.contains(residual), \"residual coercion expression `{residual}` found in src/napi.rs\");\n        }\n    }",
        "RED — Run `cargo nextest run --workspace --all-features -E 'test(module_denies_cast_lints) + test(boundary_carries_exactly_one_cast_allow_and_no_residual_coercion)'`. Expected failure: `module_denies_cast_lints` fails (the deny attribute doesn't exist yet); `boundary_carries_exactly_one_cast_allow_and_no_residual_coercion` fails its count assertion (`left: 1, right: 1` may already hold post-T3/T4 since the three rendering cast-allows were already deleted in T3/T4 — if so this half passes; the deny-attribute half still fails). Confirm at least one of the two assertions fails before proceeding.",
        "GREEN — In src/napi.rs, insert the following inner attribute immediately after the existing `#![allow(unsafe_code)]` line near the top of the file:\n\n#![deny(clippy::as_conversions, clippy::cast_possible_truncation, clippy::cast_sign_loss, clippy::cast_possible_wrap, clippy::cast_precision_loss)]",
        "GREEN — Confirm no cast-allow attributes remain in src/napi.rs other than the one on `is_retryable_status` (line ~325, resilience-domain, out of scope for this spec) by running `grep -n '#\\[allow(clippy::cast_possible_truncation' src/napi.rs` and confirming exactly one match, on the `is_retryable_status` function. If T3/T4 left any of the three rendering cast-allows behind, delete them now.",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(module_denies_cast_lints) + test(boundary_carries_exactly_one_cast_allow_and_no_residual_coercion)'`. Expected: `2 tests run: 2 passed`.",
        "GREEN — Run `just check` (fmt-check, clippy --workspace --all-features -- -D warnings, deny, typos, fmt-md-check) and confirm it exits 0."
      ],
      "commit_message": "chore(napi): deny residual cast lints and forbid open-coded numeric coercion"
    },
    {
      "id": "T6-node-totalcount-agreement",
      "title": "Node tests: totalCount rejection and cross-entry-point agreement",
      "depends_on": ["T5-lint-deny-and-residual-scan"],
      "covers_criteria": ["AC-008"],
      "files": {
        "modify": ["packages/michi-node/__test__/index.test.mjs"]
      },
      "steps": [
        "RED — Run `just build-node` to compile the addon with the current (pre-this-task) source, then append the following new describe block to the end of packages/michi-node/__test__/index.test.mjs:\n\nvoid describe('numeric boundary agreement (SPEC-ARCH-003)', () =&gt; {\n  void it('totalCount rejects out-of-domain input identically across renderToon, renderKv, and AgentResponse', () =&gt; {\n    for (const bad of [-1, 1.5]) {\n      const substring = `got ${bad}`\n      assert.throws(\n        () =&gt; renderToon({ typeName: 't', fields: ['a'], rows: [], totalCount: bad, hints: [] }),\n        (err) =&gt; err.message.includes('expected a non-negative integer no greater than 9007199254740991') &amp;&amp; err.message.includes(substring)\n      )\n      assert.throws(\n        () =&gt; renderKv([], bad, []),\n        (err) =&gt; err.message.includes('expected a non-negative integer no greater than 9007199254740991') &amp;&amp; err.message.includes(substring)\n      )\n      assert.throws(\n        () =&gt; new AgentResponse('t').totalCount(bad),\n        (err) =&gt; err.message.includes('expected a non-negative integer no greater than 9007199254740991') &amp;&amp; err.message.includes(substring)\n      )\n    }\n  })\n\n  void it('totalCount entry points agree on in-domain values including 2147483648', () =&gt; {\n    for (const good of [0, 1, 100, 2147483648]) {\n      const toonOut = renderToon({ typeName: 't', fields: ['a'], rows: [], totalCount: good, hints: [] })\n      assert.ok(toonOut.includes(`totalCount: ${good}`), `renderToon disagreed for ${good}: ${toonOut}`)\n\n      const kvOut = renderKv([{ key: 'id', value: { type: 'int', intVal: 1 } }], good, [])\n      assert.ok(kvOut.includes(`totalCount: ${good}`), `renderKv disagreed for ${good}: ${kvOut}`)\n\n      const r = new AgentResponse('t')\n      r.items([[{ type: 'int', intVal: 1 }]], ['a'])\n      r.totalCount(good)\n      const agentOut = r.renderToon()\n      assert.ok(agentOut.includes(`totalCount: ${good}`), `AgentResponse.totalCount disagreed for ${good}: ${agentOut}`)\n    }\n  })\n})\n\nRun `cd packages/michi-node &amp;&amp; pnpm test`. Expected failure: both new tests fail because the addon compiled for this RED step still predates T1-T5 (rebuild happens in the next step, which is the actual GREEN transition) — OR, if T1-T5 are already committed ahead of this task in the branch history, the RED step instead documents that this describe block is new and must pass only after `just build-node` picks up the T1-T5 source; run `just build-node &amp;&amp; cd packages/michi-node &amp;&amp; pnpm test` once now to establish the true starting state before treating the tests as red or green.",
        "GREEN — Run `just build-node` (rebuilds the addon from the current src/napi.rs, which already has T1-T5's newtypes and migrated call sites from prior tasks in this plan) then `cd packages/michi-node &amp;&amp; pnpm test`. Expected: both new tests pass, and no existing test in the file regresses.",
        "GREEN — Run `cd packages/michi-node &amp;&amp; pnpm test -- --test-name-pattern 'numeric boundary agreement'` (or equivalent grep flag for the project's node:test runner invocation) and confirm exactly 2 tests pass, 0 fail."
      ],
      "commit_message": "test(napi-node): assert totalCount rejection and cross-entry-point agreement"
    },
    {
      "id": "T7-node-rejection-tests",
      "title": "Node tests: fractional intVal, oversized decimalsVal, NaN floatVal, negative maxChars, default decimals",
      "depends_on": ["T6-node-totalcount-agreement"],
      "covers_criteria": ["AC-009"],
      "files": {
        "modify": ["packages/michi-node/__test__/index.test.mjs"]
      },
      "steps": [
        "RED — Replace the existing test at packages/michi-node/__test__/index.test.mjs:283 (`void it('AC-008 truncates a non-integer intVal toward zero with no error', () =&gt; { ... })`, inside the `describe('int64 boundary (SPEC-NAPI-POINTFIX-001)')` block) with:\n\n  void it('renderToon rejects a fractional intVal', () =&gt; {\n    assert.throws(\n      () =&gt; renderToon({ typeName: 't', fields: ['a'], rows: [[{ type: 'int', intVal: 1.5 }]], hints: [] }),\n      (err) =&gt; err.message.includes('expected an integer in [-9007199254740991, 9007199254740991], got 1.5')\n    )\n  })\n\nThen append a new describe block at the end of the file:\n\nvoid describe('rejection instead of coercion (SPEC-ARCH-003 AC-009)', () =&gt; {\n  void it('truncate rejects a negative maxChars', () =&gt; {\n    assert.throws(\n      () =&gt; truncate('hello', -5, 'full=true'),\n      (err) =&gt; err.message.includes('expected a non-negative integer no greater than 9007199254740991, got -5')\n    )\n  })\n\n  void it('renderKv rejects a decimalsVal above 20', () =&gt; {\n    assert.throws(\n      () =&gt; renderKv([{ key: 'score', value: { type: 'float', floatVal: 1.0, decimalsVal: 21 } }], null, []),\n      (err) =&gt; err.message.includes('expected an integer in [0, 20], got 21')\n    )\n  })\n\n  void it('renderToon rejects a NaN floatVal', () =&gt; {\n    assert.throws(\n      () =&gt; renderToon({ typeName: 't', fields: ['a'], rows: [[{ type: 'float', floatVal: NaN }]], hints: [] }),\n      (err) =&gt; err.message.includes('expected a finite number, got NaN')\n    )\n  })\n\n  void it('renderKv defaults absent decimalsVal to 6', () =&gt; {\n    const out = renderKv([{ key: 'score', value: { type: 'float', floatVal: 1.0 } }], null, [])\n    assert.ok(out.includes('1.000000'), `expected 6 decimal places by default, got: ${out}`)\n  })\n})\n\nRun `just build-node &amp;&amp; cd packages/michi-node &amp;&amp; pnpm test`. Expected failure at this point: none of these tests should actually fail if T1-T5's Rust changes are already built into the addon (they exercise already-implemented rejection behavior) — run this as a sanity check; if any of the five new/rewritten assertions fails, the Rust-side implementation from T1-T5 does not yet match this task's expected behavior and must be fixed before proceeding, not the test.",
        "GREEN — Run `just build-node &amp;&amp; cd packages/michi-node &amp;&amp; pnpm test`. Expected: all 5 tests (the rewritten `renderToon rejects a fractional intVal` plus the 4 new tests) pass, and the full suite shows 0 failures.",
        "GREEN — Run `grep -c \"AC-008 truncates a non-integer intVal toward zero with no error\" packages/michi-node/__test__/index.test.mjs` and confirm the output is `0` — the old test name must no longer exist in the file."
      ],
      "commit_message": "test(napi-node): assert rejection of fractional intVal, oversized decimalsVal, NaN floatVal, and negative maxChars"
    },
    {
      "id": "T8-typescript-surface-check",
      "title": "Node test: generated index.d.ts keeps plain number types",
      "depends_on": ["T7-node-rejection-tests"],
      "covers_criteria": ["AC-012"],
      "files": {
        "modify": ["packages/michi-node/__test__/index.test.mjs"]
      },
      "steps": [
        "RED — Add these two imports at the top of packages/michi-node/__test__/index.test.mjs, immediately after `import assert from 'node:assert/strict'`:\n\nimport fs from 'node:fs'\nimport path from 'node:path'\nimport { fileURLToPath } from 'node:url'\n\nconst __dirname = path.dirname(fileURLToPath(import.meta.url))\n\nThen append this describe block at the end of the file:\n\nvoid describe('index.d.ts keeps plain number types (SPEC-ARCH-003 AC-012)', () =&gt; {\n  void it('declares plain number types for all converted positions and leaks no newtype names', () =&gt; {\n    const dts = fs.readFileSync(path.join(__dirname, '..', 'index.d.ts'), 'utf8')\n    for (const expected of [\n      'intVal?: number',\n      'floatVal?: number',\n      'decimalsVal?: number',\n      'totalCount?: number',\n      'totalCount: number | undefined | null',\n      'maxChars: number',\n      'totalCount(n: number): void',\n    ]) {\n      assert.ok(dts.includes(expected), `index.d.ts missing declaration: ${expected}`)\n    }\n    for (const forbidden of ['bigint', 'BigInt', 'JsCount', 'JsInt', 'JsRanged', 'JsDecimals', 'JsFloat']) {\n      assert.ok(!dts.includes(forbidden), `index.d.ts leaked forbidden string: ${forbidden}`)\n    }\n  })\n})\n\nRun `rm -f packages/michi-node/index.d.ts &amp;&amp; cd packages/michi-node &amp;&amp; pnpm test`. Expected failure: `ENOENT: no such file or directory, open '.../packages/michi-node/index.d.ts'` — index.d.ts is a gitignored, build-generated artifact absent until `just build-node` runs, so this is the correct red state, not a false negative.",
        "GREEN — Run `just build-node` from the repo root to regenerate packages/michi-node/index.d.ts from the T1-T5 source (which already carries every `ts_type`/`ts_arg_type` override named in this spec).",
        "GREEN — Run `cd packages/michi-node &amp;&amp; pnpm test`. Expected: the new test passes, confirming index.d.ts contains all 7 exact declaration strings and none of the 7 forbidden substrings.",
        "GREEN — As an independent manual spot-check (not part of the automated suite), run `grep -n 'intVal\\|floatVal\\|decimalsVal\\|totalCount\\|maxChars' packages/michi-node/index.d.ts` and visually confirm every occurrence reads a plain `number` (or the render_kv nullable union), never `JsInt`/`JsFloat`/`JsDecimals`/`JsCount`."
      ],
      "commit_message": "test(napi-node): assert generated TypeScript surface keeps plain number types"
    },
    {
      "id": "T9-docs-and-supersession",
      "title": "Rewrite numeric boundary docs and record spec supersession",
      "depends_on": ["T8-typescript-surface-check"],
      "covers_criteria": ["AC-013"],
      "files": {
        "modify": [
          "docs/spec/04-mcp-and-napi.md",
          ".claude/specs/structural-napi-numeric-boundary-narrow-inconsistent-i32-coercion-across-multiple-call-sites.json",
          ".claude/specs/napi-boundary-point-fixes.json",
          "src/napi.rs"
        ]
      },
      "steps": [
        "RED — Append the following test to the `#[cfg(test)] mod tests { ... }` block in src/napi.rs:\n\n    #[test]\n    fn napi_numeric_docs_and_superseded_specs_are_consistent() {\n        let docs = include_str!(\"../docs/spec/04-mcp-and-napi.md\");\n        assert!(!docs.contains(\"clamped non-negative\"), \"docs still describe clamping\");\n        assert!(!docs.contains(\"n.max(0) as usize\"), \"docs still show the deleted clamp expression\");\n        assert!(!docs.contains(\"silently lose precision\"), \"docs still describe silent precision loss\");\n        for field in [\"totalCount\", \"decimalsVal\", \"maxChars\", \"intVal\", \"floatVal\"] {\n            assert!(docs.contains(field), \"numeric boundary docs missing rejection contract for {field}\");\n        }\n\n        let arch002: serde_json::Value = serde_json::from_str(include_str!(\n            \"../.claude/specs/structural-napi-numeric-boundary-narrow-inconsistent-i32-coercion-across-multiple-call-sites.json\"\n        ))\n        .expect(\"SPEC-ARCH-002 is valid JSON\");\n        let purpose = arch002[\"purpose\"].as_str().expect(\"purpose is a string\");\n        assert!(purpose.starts_with(\"SUPERSEDED BY SPEC-ARCH-003\"), \"got: {purpose}\");\n\n        let point_fixes: serde_json::Value =\n            serde_json::from_str(include_str!(\"../.claude/specs/napi-boundary-point-fixes.json\"))\n                .expect(\"napi-boundary-point-fixes.json is valid JSON\");\n        let criteria = point_fixes[\"acceptance_criteria\"].as_array().expect(\"acceptance_criteria is an array\");\n        let superseded_ids = [\"AC-007\", \"AC-008\", \"AC-011\"];\n        for c in criteria {\n            let id = c[\"id\"].as_str().expect(\"id is a string\");\n            let criterion = c[\"criterion\"].as_str().expect(\"criterion is a string\");\n            if superseded_ids.contains(&amp;id) {\n                assert!(\n                    criterion.starts_with(\"SUPERSEDED BY SPEC-ARCH-003 -- \"),\n                    \"{id} should carry the supersession prefix, got: {criterion}\"\n                );\n            } else {\n                assert!(\n                    !criterion.starts_with(\"SUPERSEDED BY SPEC-ARCH-003\"),\n                    \"{id} should not carry the supersession prefix\"\n                );\n            }\n        }\n    }",
        "RED — Run `cargo nextest run --workspace --all-features -E 'test(napi_numeric_docs_and_superseded_specs_are_consistent)'`. Expected failure: assertion failure on the first check (`docs still describe clamping` — 'clamped non-negative' is still present in docs/spec/04-mcp-and-napi.md) before any doc/spec edits are made.",
        "GREEN — In docs/spec/04-mcp-and-napi.md, replace the entire '### Numeric boundary' section (from the `### Numeric boundary` heading at line 162 through the paragraph ending '...silently lose precision per Node's N-API coercion behavior.') with:\n\n### Numeric boundary\n\nEvery JavaScript number that crosses into a rendering call is validated at the boundary through a newtype in `src/napi/num.rs` before any michi code runs; an out-of-domain value throws a JS `Error` instead of being narrowed. `napi_get_value_double` (a non-coercing read) is used for every converted position, so `napi_get_value_int32`'s `ToInt32` wraparound is never in the path.\n\n- `totalCount` (`JsToonOptions.totalCount`, `renderKv`'s `totalCount` parameter, and `AgentResponse.totalCount`) is rejected unless it is a finite, non-negative integer no greater than `Number.MAX_SAFE_INTEGER` (2^53 - 1).\n- `decimalsVal` is rejected unless it is a finite integer in `[0, 20]`.\n- `maxChars` (`truncate`'s parameter) is rejected unless it is a finite, non-negative integer no greater than `Number.MAX_SAFE_INTEGER`.\n- `intVal` is rejected unless it is a finite integer in `[-9007199254740991, 9007199254740991]` — the range over which a JS number and an `i64` agree exactly.\n- `floatVal` is rejected unless it is finite; `NaN`, `Infinity`, and `-Infinity` are all rejected.\n\nNone of these five inputs is ever silently clamped, truncated, or defaulted on an out-of-domain value — rejection happens before any michi rendering code runs.",
        "GREEN — In .claude/specs/structural-napi-numeric-boundary-narrow-inconsistent-i32-coercion-across-multiple-call-sites.json, prepend `\"SUPERSEDED BY SPEC-ARCH-003 (rendering) and its resilience-domain sibling -- never implemented. \"` to the start of the `\"purpose\"` field's string value (leave the rest of the purpose text unchanged).",
        "GREEN — In .claude/specs/napi-boundary-point-fixes.json, prepend `\"SUPERSEDED BY SPEC-ARCH-003 -- \"` to the start of the `\"criterion\"` string value for the objects whose `\"id\"` is `\"AC-007\"`, `\"AC-008\"`, and `\"AC-011\"` (leave the rest of each criterion's text, and all other criteria, unchanged).",
        "GREEN — Run `cargo nextest run --workspace --all-features -E 'test(napi_numeric_docs_and_superseded_specs_are_consistent)'`. Expected: `1 test run: 1 passed`.",
        "GREEN — Run `just snapshots` (via `cargo insta test` non-interactively) and confirm exit status 0 with zero diffs against tests/snapshots/.",
        "GREEN — Run `cd packages/michi-node &amp;&amp; pnpm test` one final time and confirm `AC-002 renders a negative int beyond i32::MIN losslessly` still yields `t[1]{a}:\\n  -1755000000000\\n` and `AC-003 renderKv renders an int beyond i32::MAX losslessly` still yields `id: 1755000000000\\n`, unchanged from before this spec's work began."
      ],
      "commit_message": "docs(napi): rewrite numeric boundary contract and record spec supersession"
    }
  ]
}
```

That is the full, unabridged content of the file — all 9 tasks (T1–T9), verbatim.</result>
<usage><subagent_tokens>52644</subagent_tokens><tool_uses>0</tool_uses><duration_ms>141406</duration_ms></usage>
</task-notification>

---

I feel like we need to be a lot more thorough with our TDD because we've had a lot of regressions that should have been cought by the tests and our specs and were missed by the tests and barely caught by specs. How do you plan on doing that?

---

Shall we save everything we need to start with a new session? our context is massive right now

---

What prompt should we give the new session?