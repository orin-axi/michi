# michi — Specification

> This file has been restructured into [`docs/spec/`](spec/README.md) for readability — the
> content moved, none of it changed. Start at [`docs/spec/README.md`](spec/README.md) for the
> reading order.

Kept as a redirect rather than deleted, so any existing link to this path still lands somewhere
useful.
