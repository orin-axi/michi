[package]
name        = "michi-truncate"
version     = "0.1.0"
description = "UTF-8 char-boundary safe string truncation primitives for michi and AXI"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[dependencies]

[dev-dependencies]
trybuild = "1"
