[package]
name        = "michi-resilience"
version     = "0.1.0"
description = "Retry delay calculation, RFC 7231 Retry-After parsing, and FNV-1a idempotency primitives"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[dependencies]

[dev-dependencies]
trybuild = "1"
